# Restart PostgreSQL
sudo service postgresql restart

# Backend setup
cd server
npm install
npx prisma migrate dev --name init
npx prisma generate
npm run prisma:seed
npm run dev

#Real data sync
# Sync a single company
curl -X POST http://localhost:5000/api/v1/sync/company/APN

# Sync all companies (takes ~15 seconds)
curl -X POST http://localhost:5000/api/v1/sync/all

# Frontend setup (new terminal)
cd ../client
npm install
npm run dev

#Github 
# 1. Make changes to your code...
# 2. Add the changes to be committed
     git add .
# 3. Commit the changes with a clear message
     git commit -m "feat: describe what you changed"
# 4. Push the changes to GitHub
     git push

NEXT TO DO LIST
Now that the Dashboard and Company Profile frontend is in place, let's ensure all prices and figures are pulling correctly from the FMP API — currently every value is displaying as zero.
Please do a full scan of the entire app to identify every point where mock or placeholder data is being used, and replace it all with live API data. The goal is a fully functional application with real data throughout — no exceptions.
Once this is fixed and verified, we'll move on to the next item on the list

