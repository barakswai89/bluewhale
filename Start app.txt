# Restart PostgreSQL
sudo service postgresql restart

# Backend setup
cd server
npm install
npx prisma migrate dev --name init
npx prisma generate
npm run prisma:seed
npm run dev

#Real data sync
# Sync a single company
curl -X POST http://localhost:5000/api/v1/sync/company/APN

# Sync all companies (takes ~15 seconds)
curl -X POST http://localhost:5000/api/v1/sync/all

# Frontend setup (new terminal)
cd ../client
npm install
npm run dev

#Github 
# 1. Make changes to your code...
# 2. Add the changes to be committed
     git add .
# 3. Commit the changes with a clear message
     git commit -m "feat: describe what you changed"
# 4. Push the changes to GitHub
     git push

NEXT TO DO LIST
The railway log integration is correctly rendering price data.
**************************************************************************************************************************************************************************************************************
On the screener table view, there is a formatting defect in the ZAR currency presentation. The “R” symbol is not behaving as a proper prefix for high-value numbers and is breaking visual alignment by rendering above the numeric value instead of inline.

Refactor the currency formatting layer so that the symbol is bound to the value at the formatting/serialization level, not the presentation layer. The output must be a single atomic string (e.g. R10,000.00) with no positional separation or layout dependency. CSS fixes are not an acceptable solution here.
**************************************************************************--------------------------------**********************************************************************************************************************************
On the company profile page, the current data ingestion scope is insufficient.

Extend the scraping/aggregation pipeline to include:

Additional structured company metadata beyond the current schema
On the company profile page, the current data ingestion scope is insufficient.
Extend the scraping/aggregation pipeline to include:

* Additional structured company metadata beyond the current schema
* Live or frequently refreshed news ingestion
* Recent activity and profile update streams
The profile service must shift from static snapshot retrieval to a continuously refreshable or regularly revalidated data model to ensure recency of information.
Implement these changes before proceeding to the next item.



ENVIRONMENTAL VARIABLES
DATABASE_URL="postgresql://postgres:sKHlByrTJLYGNtDzMjzYReNFsbSfisUy@thomas.proxy.rlwy.net:57170/railway"
DIRECT_URL="postgresql://postgres:sKHlByrTJLYGNtDzMjzYReNFsbSfisUy@thomas.proxy.rlwy.net:57170/railway"
JWT_SECRET="ace3d373ec6962f4fc3bc9f0bd6246ae7ae96273456c4b5ab2fac15f358df185"
JWT_EXPIRES_IN="7d"
FMP_API_KEY="BdUA2UApEhod66FGG6zSTSFeodCU6qKT"
CLIENT_URL="https://bluewhale-beta.netlify.app"
CORS_ORIGIN="https://bluewhale-beta.netlify.app"
NODE_ENV="production"
ANTHROPIC_API_KEY=""